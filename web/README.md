# HRRecruit Web Portal

React web portal for HRRecruit recruiters, interviewers, and hiring managers.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` to the Django API base URL.
