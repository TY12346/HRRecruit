# HRRecruit Backend Dependencies
# Location: backend/requirements.txt
#
# Install with:
#   pip install -r requirements.txt

# Core backend framework
Django>=5.2,<6.0
djangorestframework>=3.16,<4.0

# JWT authentication
djangorestframework-simplejwt>=5.5,<6.0

# CORS support for React/Flutter API access
django-cors-headers>=4.7,<5.0

# PostgreSQL database driver
psycopg2-binary>=2.9,<3.0

# Environment variable support
python-dotenv>=1.0,<2.0

# Image/file handling support
Pillow>=11.0,<12.0

# Resume file parsing
# Optional NLP-assisted skill extraction; install the model separately with:
#   python -m spacy download en_core_web_sm
spacy>=3.7,<4.0
PyMuPDF>=1.24,<2.0
python-docx>=1.1,<2.0

# PDF report generation
reportlab>=4.2,<5.0


# Optional real payment gateway support (sandbox mode)
stripe>=10.0,<11.0
# Optional trained resume-matching ML pipeline
scikit-learn>=1.5,<2.0
joblib>=1.4,<2.0
# torch<2.3 wheels were compiled against the NumPy 1.x C API. NumPy 2.x can
# therefore emit _ARRAY_API errors while spaCy imports torch.
numpy==1.26.4
sentence-transformers>=3.0,<4.0

# Real local Whisper transcription support.  The model is selected by
# WHISPER_MODEL_SIZE (small by default); this does not use a hosted ASR API.
openai-whisper==20250625

# Real pyannote 3.1 diarization dependencies.  This compatible set avoids the
# transformers 4.57 lazy-import regression that surfaces as an AutoModel error.
# A Hugging Face token with accepted model terms is still required at runtime.
torch==2.2.2
torchaudio==2.2.2
pyannote.audio==3.1.1
transformers>=4.35,<4.42
huggingface_hub>=0.20,<0.25

# Optional real OpenAI transcription/summary support
openai>=1.90,<2.0

# Optional real Google Gemini summary support
google-genai>=1.0,<2.0

# Firebase Cloud Messaging push notifications
firebase-admin>=6.5,<7.0

# Optional real Google Calendar OAuth/event sync
google-api-python-client>=2.130,<3.0
google-auth>=2.30,<3.0
google-auth-oauthlib>=1.2,<2.0
